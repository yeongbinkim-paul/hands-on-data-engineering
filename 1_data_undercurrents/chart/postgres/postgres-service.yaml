# Source: airflow/charts/postgresql/templates/svc-headless.yaml
apiVersion: v1
kind: Service
metadata:
  name: "postgresql-headless-maindb"
  namespace: "maindb"
  labels:
    app: "postgresql-maindb"
spec:
  type: ClusterIP
  clusterIP: None
  ports:
    - name: postgresql-maindb
      port: 5432
      targetPort: 5432
  selector:
    app: "postgresql-maindb"
---
# Source: airflow/charts/postgresql/templates/svc.yaml
apiVersion: v1
kind: Service
metadata:
  name: "postgresql-maindb"
  namespace: "maindb"
  labels:
    app: "postgresql-maindb"
spec:
  type: ClusterIP
  ports:
    - name: postgresql-maindb
      port: 5432
      targetPort: 5432
  selector:
    app: "postgresql-maindb"
